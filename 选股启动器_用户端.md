---
title: 高息股选股看板（用户端）
cssclass: dashboard
update: 自动（基于股票池 frontmatter）
---





# 选股系统启动台（用户端）

**免责申明**：**此工具仅用来帮助筛选股票，筛选逻辑由使用者自己设置，并不会推荐任何股票给使用者，筛选结果也请使用者注意分辨数据正确性，所有损失，工具概不负责。理财有风险，投资需谨慎。
使用此工具，默认同意此免责申明。

> 本启动器无需 Tushare API token，所有数据从 OSS 拉取。



---

## 一键执行

```button
name ▼ 从OSS拉取今日数据
type command
action Templater: Open Insert Template modal
color blue
```

```button
name ▶ 运行每日筛选入池
type command
action Templater: Open Insert Template modal
color green
```

```button
name 📈 生成趋势图
type command
action Templater: Open Insert Template modal
color purple
```

> **顺序**：先点「从OSS拉取今日数据」下载归档文件，再点「运行每日筛选入池」执行筛选。
> 调整筛选参数后只需重新运行筛选即可，无需重新拉取。
> 对感兴趣的股票，可运行「生成趋势图」查看历史走势，或运行 **Templates/个股分析模板** 创建深度分析笔记。
> 模板在 **用户端** 子文件夹下。

---

## 快速导航

- 📂 [[Templates/选股看板|打开选股看板]]
- ⚙️ [[选股配置|调整筛选参数]]
- 📊 `$= dv.paragraph(dv.fileLink('每日分析结果/' + dv.date('now').toFormat("yyyy年MM月dd日") + '/股息率排名', false, '查看今日排名'))`
- 📋 `$= dv.paragraph(dv.fileLink('每日分析结果/' + dv.date('now').toFormat("yyyy年MM月dd日") + '/选股变动', false, '查看今日变动'))`
- 📈 `$= dv.paragraph(dv.fileLink('每日分析结果/' + dv.date('now').toFormat("yyyy年MM月dd日") + '/趋势图', false, '查看趋势图'))`

> 如果今日尚未运行脚本，链接会显示为空白，运行后自动生效。

---

## 使用流程

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 打开 **[[选股配置\|选股配置]]** 修改筛选参数 | 无需填写 apiToken |
| 2 | 点击 **▼ 从OSS拉取今日数据** | 选择「用户端 → 每日数据拉取脚本」 |
| 3 | 点击 **▶ 运行每日筛选入池** | 选择「用户端 → 每日筛选脚本」 |
| 4 | 随时打开 **[[Templates/选股看板\|选股看板]]** 查看选股池 | Dataview 仪表盘 |

> 与开发端的区别：用户端不直接调用 Tushare API，所有数据来自开发端预先生成并上传到 OSS 的归档文件。

---

## OSS 配置

在 `选股配置.md` frontmatter 中设置：

```yaml
ossUrl: "https://your-bucket.oss-cn-xxx.aliyuncs.com/stock-archive"
archiveRetryDays: 5
```

> 首次使用需要开发端提供 OSS 地址。联系开发端获取。
