---
baseFolder: 股票历史记录
poolFolder: 股票池
reportFolder: 每日分析结果
minDividend: 4.5
excludeNegativePE: true
excludeNegativeROE: true
maxDebtRatio: 60
minMarketCapYi: 20
scoringDividendWeight: 30
scoringPEWeight: 15
scoringPBWeight: 5
scoringDebtWeight: 10
scoringMarketCapWeight: 10
scoringROEWeight: 15
scoringProfitGrowthWeight: 10
scoringEPSWeight: 5
dividendNormCeil: 15
peNormMax: 20
pbNormMax: 5
roeNormMax: 30
profitGrowthNormRange: 50
epsNormMax: 5
targetDate:
archiveRetryDays: 5
ossUrl: https://stocktool.oss-cn-hangzhou.aliyuncs.com
---
**



# ⚙️ 选股控制面板

**免责申明**：**此工具仅用来帮助筛选股票，筛选逻辑由使用者自己设置，并不会推荐任何股票给使用者，筛选结果也请使用者注意分辨数据正确性，所有损失，工具概不负责。理财有风险，投资需谨慎。**
**使用此工具，默认同意此免责申明**
修改上方 frontmatter 中的参数即可调整选股策略，保存后运行「每日筛选入池」脚本即可生效。

## 筛选参数

| 参数 | 当前值 | 说明 |
|------|--------|------|
| `minDividend` | 3.8 | 股息率低于此值的股票不入池 |
| `excludeNegativePE` | true | 关闭后亏损股（PE为负）也会入池 |
| `excludeNegativeROE` | true | 关闭后 ROE 为负的亏损公司也会入池 |
| `maxDebtRatio` | 60 | 负债率超过此值会标 ⚠️ 警告，但仍入池 |
| `minMarketCapYi` | 20 | 市值低于此值的股票不入池 |

## 评分权重

系统为入池股票计算综合评分（0-100），用于排名和筛选。各因子权重可调：

| 参数 | 当前值 | 因子 | 说明 |
|------|--------|------|------|
| `scoringDividendWeight` | 30 | 股息率 | 越高越好，minDividend%~dividendNormCeil% 映射到 0~100 分 |
| `scoringPEWeight` | 15 | 市盈率 | 越低越好，peNormMax~0 映射到 0~100 分 |
| `scoringROEWeight` | 15 | ROE | 净资产收益率，0%~roeNormMax% 映射到 0~100 分 |
| `scoringMarketCapWeight` | 10 | 总市值 | 越大越好，10亿~5000亿对数映射 |
| `scoringDebtWeight` | 10 | 负债率 | 越低越好，100%~0% 映射到 0~100 分 |
| `scoringProfitGrowthWeight` | 10 | 净利同比增长 | 越高越好，-profitGrowthNormRange%~+profitGrowthNormRange% 映射到 0~100 分 |
| `scoringPBWeight` | 5 | 市净率 | 越低越好，pbNormMax~0 映射到 0~100 分 |
| `scoringEPSWeight` | 5 | 每股收益(EPS) | 0~epsNormMax 元映射到 0~100 分 |

权重之和不必是 100，系统会自动归一化。设 `0` 可关闭某因子。

## 评分归一化参数

以下参数控制各因子映射到 0~100 分时的上下界，可根据市场环境调整：

| 参数 | 当前值 | 说明 |
|------|--------|------|
| `dividendNormCeil` | 15 | 股息率映射上限(%)，超过此值均得 100 分 |
| `peNormMax` | 20 | PE 映射上限，PE 超过此值得 0 分（越低越好） |
| `pbNormMax` | 5 | PB 映射上限，PB 超过此值得 0 分（越低越好） |
| `roeNormMax` | 30 | ROE 映射上限(%)，ROE 超过此值得 100 分 |
| `profitGrowthNormRange` | 50 | 净利增长映射范围(±%)，范围外得 0 或 100 分 |
| `epsNormMax` | 5 | EPS 映射上限(元)，EPS 超过此值得 100 分 |

> 股息率下限自动使用 `minDividend`，无需单独配置。

## OSS 分发参数

| 参数 | 当前值 | 说明 |
|------|--------|------|
| `ossUrl` | — | OSS 基础 URL，不含文件名。如 `https://bucket.oss-cn-xxx.aliyuncs.com/stock-archive` |
| `archiveRetryDays` | 5 | 拉取数据时向前重试天数 |
| `targetDate` | （空=今天） | 指定归档日期，格式 YYYYMMDD（如 20260506）。留空默认今天 |

> 开发端生成归档文件后上传到此 OSS 地址，用户端通过「每日数据拉取脚本」下载。留空则不启用 OSS 分发。

## 快速预设

| 方案 | minDividend | maxDebtRatio | minMarketCapYi | 风格 |
|------|-------------|-------------|---------------|------|
| 稳健型 | 5.0 | 50 | 50 | 保守 |
| 进取型 | 4.0 | 80 | 5 | 激进 |
| 当前 | 3.8 | 60 | 20 | 均衡 |

---

_最后修改: `$= dv.date('now')`_
